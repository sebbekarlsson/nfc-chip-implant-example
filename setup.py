from setuptools import setup


setup(
    name='nfc-chip-implant-example',
    version='1.0',
    install_requires=[
        'nfcpy'
    ],
    packages=[
    ],
    entry_points={
        'console_scripts': [
        ]
    }
)
